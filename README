To build the Framework and Bundle targets, you must do the following:

- Download the TagLib v1.7 sources from http://developer.kde.org/~wheeler/taglib.html
- Unpack the sources into a folder inside this XCode project called 'taglib-src'

